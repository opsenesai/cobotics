import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "New | Console",
}

export default function NewPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">New</h1>
}
