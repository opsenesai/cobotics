import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Plugins | Console",
}

export default function PluginsPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Plugins</h1>
}
