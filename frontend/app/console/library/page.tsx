import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Library | Console",
}

export default function LibraryPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Library</h1>
}
