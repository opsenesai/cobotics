import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Recents | Console",
}

export default function RecentsPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Recents</h1>
}
